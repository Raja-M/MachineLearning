import matplotlib.pyplot as plt
import numpy as np

# a = np.array([1, 2, 3, 4, 5, 10, 6, 15,9, 10])

# plt.plot(a)

# plt.show()


# x = np.linspace(0, 5, 20)

# y = np.linspace(0, 10, 20)

# plt.plot(x, y, 'purple') 
# plt.plot(x, y, 'o') 

# plt.show()

fig = plt.figure()
ax = fig.add_subplot(projection='3d')
X = np.arange(-5, 5, 0.15)
Y = np.arange(-5, 5, 0.15)
X, Y = np.meshgrid(X, Y)
R = np.sqrt(X**2 + Y**2)
Z = np.sin(R)

ax.plot_surface(X, Y, Z, rstride=1, cstride=1, cmap='viridis')

plt.show()

#write code read textfield from html page
 
